#!/bin/bash

# By default, this script will build for every version of Python enabled in your
# versionset.

# To only build for CPython, uncomment this line (this will block PyPy and Jython builds):
# [[ $1 == CPython* ]] || exit 1

# To only build for CPython3/PyPy3, uncomment this line:
# [[ $1 == *Py*3* ]] || exit 1

# To only build for 2.7 interpreters, uncomment this line:
# [[ $1 == *27* ]] || exit 1

# To turn off building for Jython, uncomment this line:
# ([[ $1 == Jython* ]] && exit 1) || true
